# 時值 · 財務地圖 — 產品規劃書暨實作說明

> 版本：**v3.4**（取代 v3.3）　｜　文件性質：產品規劃 + 實作現況 + 商業化策略
> 產品代稱：**時值 · 財務地圖**
> 本版重點：
> 1. **Landing Page（`landing.html`）** 正式上線：Hero、痛點、功能介紹、三方案定價卡、FAQ、頁尾 CTA；所有按鈕導向 `index.html#login`。
> 2. **使用者條款與隱私政策（`terms.html`）** 獨立頁面：禁止行為、反盜版條款、賠償責任限制、服務可用性、通知方式；`#privacy` 錨點分隔。
> 3. **建議報告付費牆**：免費用戶僅看第一節，第二節起漸層模糊 + 升級 CTA（純前端 CSS 遮罩）。
> 4. **商業化 UX 落地**：升級 Modal 標題動態化、側邊欄方案狀態 Widget（永久可見）、`goToLogin()` 滾動至雲端同步區、登入後自動重繪當前分頁。
> 5. **模組商店** 對所有用戶開放瀏覽（含未登入），購買限制移至內部提示；加購模組所有權獨立於訂閱，永久有效。
> 6. **預設值更新**：`DEFAULT_SETTINGS` 依主計總處 25–29 歲薪資統計調整；郵局模組預設為專業職(二)·薪級55·職層3·115年入局；`seedSample()` 更新為 25–28 歲財務樣貌。
> 7. **管理後台 (`admin.html`)** 新增「追蹤更新通知連結」設定群組；定價完全以 Supabase `fp_site_config` 為準（`admin.html → 站點設定 → 方案定價` 調整）。

---

## 0. 摘要與實作現況

「時值」是一個個人財務試算與規劃面板：輸入收支、資產負債、目標、子女、分期採購、（選用）郵局薪級等資料後，能算出淨值、現金流、儲蓄率、退休準備度，並用「人生資產時間軸」推估未來各年齡的可投資資產，最後輸出一份可列印／存 PDF 的建議報告。

**目前狀態：已具備完整商業化前台（Landing Page、條款、付費牆），尚未串接金流，付款為人工確認流程。**

**已實作（可用）：** 三個主要檔案（`index.html`、`landing.html`、`terms.html`），共 **15 個功能分頁**；資料層支援兩種模式：
- **本地模式**：零設定，資料存於瀏覽器 localStorage。
- **雲端模式**：透過 Supabase（PostgreSQL + Auth + RLS）跨裝置同步。

**關鍵定位決策（不變）：** 以「試算／規劃／教育工具」定位，建議報告聚焦「財務行為與規劃原則」；投資配置頁僅提供**資產「類別」層級**的參考，**刻意不推介個股或特定商品**。

### 0.1 v3.3 → v3.4 變更摘要
| 項目 | 變更 | 對應章節 |
|---|---|---|
| **Landing Page**（全新） | `landing.html`：Hero（APP 預覽 Mock）、三痛點卡、6 功能卡、三方案定價卡 + 比較表、FAQ（`<details>`）、頁尾 CTA；設計語言與 index.html 一致 | 6、8 |
| **使用者條款與隱私政策**（全新） | `terms.html`：條款＋隱私政策（`#privacy`）獨立頁；新增「禁止行為」「反盜版」「服務可用性」「賠償責任限制」「一般條款」「通知方式」；買斷明確為「功能使用授權」不含原始碼；法院管轄：臺灣高雄地方法院 | 8、9 |
| **建議報告付費牆** | 免費用戶僅見第一節「財務現況體檢」；第二節起漸層模糊 + 升級 CTA（`showPaymentUpgrade('report')`）；付費牆採純前端 CSS 遮罩（報告內容由自身資料產生，無資料洩漏風險） | 2、8 |
| **升級 Modal 動態標題** | `UPGRADE_COPY` 物件依觸發點（`report`/`report_export`）切換標題與說明文字 | 2、8 |
| **方案狀態 Widget（永久）** | 側邊欄 nav 底部新增 `#plan-widget`：免費/未登入→顯示「升級完整版」＋「訂閱制」按鈕；付費方案→綠色 ✅ badge；切換任何分頁皆可見 | 2、6 |
| **goToLogin() 滾動優化** | 所有「前往登入」按鈕（含模組商店、升級 modal）呼叫 `goToLogin()`，點擊後切換設定頁並 scrollIntoView 至雲端同步區 | 6 |
| **登入後自動重繪** | `onAuthStateChange` 成功後加呼叫 `rerenderActiveTab()`，不需手動切換分頁即可看到更新內容 | 6 |
| **模組商店全開放** | 所有用戶（含未登入/免費）可瀏覽模組清單；購買需升級的提示移至內部；`canAccess` 繞過改為 Tab 層直通；logout 狀態 reset 保留 `addon-store` | 2 |
| **加購模組所有權永久** | 加購記錄存於 `fp_user_addons`（獨立於訂閱）；`hasAddon()` 不看當前方案；訂閱到期後加購模組仍可用，UI 已加入說明 | 4、8 |
| **註冊同意機制** | 註冊表單新增兩個 checkbox：「同意使用者條款與隱私政策」（必填）、「同意接收產品更新與優惠通知」（選填）；行銷同意存入 Supabase user metadata `marketing_consent` | 5、9 |
| **`#login` 錨點路由** | 外部連結可用 `index.html#login` 直接跳至設定頁雲端同步區（Landing Page 所有 CTA 均使用此路徑） | 6 |
| **登出清除 localStorage** | 按登出同時執行 `localStorage.removeItem(LS_KEY)`，本地暫存資料一併清除 | 5 |
| **DEFAULT_SETTINGS 更新** | 依主計總處 2023 年 25–29 歲薪資統計：current_age 27、retirement_age 65、salary_growth 3%、expected_return 6%、monthly_invest NT$3,000、勞保薪資 NT$34,800、勞保年數 38 年 | 3 |
| **DEFAULT_POSTAL 更新** | 專業職(二)·薪級55·職層3·115年入局（新進人員初始值） | 3.16 |
| **seedSample() 更新** | 25–28 歲典型財務樣本：月薪 NT$34,000、五項支出共 NT$21,000、緊急備用金 NT$12 萬 + ETF NT$5 萬、就學貸款 NT$15 萬、購屋目標 NT$150 萬/10 年 | 6 |
| **admin.html 追蹤通知設定** | 新增「📡 模組更新通知」群組（`site.newsletter_url`）；Supabase 補上 label/note；開發中模組的「追蹤更新通知」按鈕自動使用此設定 | 8 |
| **定價統一至 Supabase** | 前端 `PAYMENT_CONFIG` 預設值僅為 fallback；實際定價完全由 `fp_site_config`（`payment.price_full`/`payment.price_sub`）控制，透過 admin.html 調整 | 8 |
| **Landing Page 按鈕調整** | 完整版按鈕：「立即升級」→「最划算方案」；訂閱制按鈕：「了解訂閱制」→「立即訂閱」 | 8 |
| **隱私政策措辭** | 移除「本服務不使用 GA/FB Pixel」硬性聲明，改為「若未來引入分析工具將告知並取得同意」 | 9 |

---

## 1. 產品定位與目標

### 1.1 願景
讓一般人不需財務背景，也能在十幾分鐘內看清財務全貌、設定人生目標，並透過視覺化時間軸理解：照目前的收入、支出與投資假設，未來每年大概會有多少錢、目標能不能達成、缺口多大、該怎麼調整。

### 1.2 目標使用者
**主要受眾：25–28 歲社會新鮮人 / 職場早期階段**（依主計總處 2023 年 25–29 歲平均月薪 NT$35,411 設定預設值）；規劃買房／結婚／生育的家庭；想試算「幾歲能退休」的上班族；**公部門 / 中華郵政薪級制員工**；想做一次完整財務體檢的人。

### 1.3 核心價值主張
1. **看得懂的全貌**：一頁看清收支、淨值、儲蓄率、目標進度、退休準備度。
2. **算得出的未來**：以複利與雙資金池現金流模型，推算任意未來年度資產。
3. **分得清「能負擔」與「會卡現金」**：平均結餘看可行性、流量表看流動性。
4. **假設透明、可調整**：報酬率、通膨、薪資成長全部可調。
5. **可行動的報告**：產出以行為與原則為主的書面建議報告。

### 1.4 定位與法規對照（與第 9 章連動）
| 定位 | 內容 | 法規風險 | 本產品現況 |
|---|---|---|---|
| A. 財務試算／規劃工具 | 收支、淨值、目標、退休、現金流推估；報酬率為可調假設 | 低 | ✅ 已實作 |
| B. 資產「類別」配置參考 | 由目標反推所需報酬，對應股／債／現金比例（不指名標的） | 低～中（收費＋個人化趨近機器人理財，須留意） | ✅ 已實作為「教育級」工具 |
| C. 推介個股／特定基金 ETF | 指名「該買哪一檔」 | 高（屬投顧業務，須執照） | ⛔ 刻意不做 |

---

## 2. 已實作功能（15 個分頁）

1. **儀表板**：四張 KPI 卡（淨值、平均每月結餘＋流量最緊月份、儲蓄率、緊急預備金月數）＋「人生資產時間軸」折線圖＋退休準備度快照＋**蒙地卡羅退休模擬**（800 次模擬扇形圖、退休成功機率、順序風險說明）。
2. **收支**：收入／支出表；下方兩張流量圖——本月資金流量（典型月，滾動餘額）；全年資金流量（各月 12 根長條圖、最緊月份警示）。
3. **資產負債**：資產／負債表；資產可標記「可動用」與「不計退休」（計入淨值但不入退休推估）。
4. **目標**：多目標清單，含每月應投入、可行性判斷；支援自訂報酬率與週期型目標。
5. **子女養育**：依人生階段估算養育成本；費用編輯器分支出與政府補助（一次性/按月/教育補助）。
6. **分期採購**：月付金試算、利息試算，完整折入時間軸。
7. **郵局薪資**：中華郵政職階薪級制（114/1/1 待遇表）；碰頂機制、5年跳級、年收入加總。**預設：專業職(二)·薪級55·職層3·115年入局。**
8. **勞退勞保**：勞保老年年金（兩式擇優）＋勞退個人專戶；啟用後計入退休推估。
9. **保險防護**：壽險保障缺口（遺族需求法）；資料自動帶入。
10. **投資配置**：5 類資產滑桿式配置（股/債/REITs/黃金/現金）；所需報酬反推；四階段風險提示。
11. **建議報告**：四段式報告。**免費用戶僅可看第一節（財務現況體檢）**，第二節起以漸層遮罩鎖定並引導升級；完整報告可列印／另存 PDF（付費功能）。
12. **設定**：計算假設；雲端同步（登入/註冊/登出/修改密碼/忘記密碼）；JSON 備份；資料清除。
13. **歷史規劃紀錄**（訂閱功能）：儲存完整 state 快照；瀏覽/還原/刪除；資料存 `fp_snapshots`（RLS 保護）。
14. **模組商店**（所有用戶可瀏覽）：展示可購買模組（含開發中）；未登入/免費用戶可看清單但需升級才能加購；**已購模組永久有效，不受訂閱狀態影響**。
15. **創業財務模板**（加購模組，`startup`）：燒錢率、損益平衡點、收入目標追蹤、里程碑清單；資料與主規劃獨立。

**開發中加購模組（可在模組商店追蹤）：**
- 房地產投報試算（`realestate`）：租金投報率、貸款壓力測試、持有成本與增值試算
- 綜合所得稅試算（`itax`）

> **側邊欄方案狀態 Widget（v3.4 新增）**：固定顯示於 nav 底部，免費/未登入用戶看到「升級完整版」＋「訂閱制」兩個按鈕，切換任何分頁皆可見。付費用戶顯示綠色 ✅ 方案 badge。
>
> **側邊欄動態分組**：主要功能 / 進階模組 / 訂閱功能 / 加購模組；未開放 tab 顯示 🔒。

---

## 3. 財務計算方法論（以實際實作為準）

### 3.1 收支與儲蓄率
```
單項年化金額 = 金額 × { 每月:12, 每季:4, 每年:1 }
平均每月結餘 = 每月收入 − 每月支出 − 每月負債還款（僅計尚有餘額之負債）
儲蓄率       = (年收入 − 年支出 − 年還款) / 年收入
```

### 3.2 淨值
```
淨值 = 總資產 − 總負債          ← 含標記「不計退休」的資產
緊急預備金（月） = 可動用資產 / 每月支出
```

### 3.3 貨幣時間價值
```
終值 FV = PV × (1+r)^n ；現值 PV = FV / (1+r)^n
年金終值 = PMT × [((1+r)^n − 1)/r]
```

### 3.4 通膨與實質金額
```
未來名目 = 今日金額 × (1+通膨)^n
```

### 3.5 目標所需提撥（含自訂報酬率＋週期）
```
目標報酬率 = 該目標自訂報酬率（若有填）否則沿用全域
達標每月投入 PMT = 剩餘目標 × rm / ((1+rm)^n − 1)；週期型 n=週期×12
```

### 3.6 目標瀑布式分配（報告頁）
依優先序逐一扣抵可投入，計算預估達成年數。

### 3.7 人生資產時間軸 — 雙資金池模型
```
投資池（不可動用資產）以預期報酬複利；現金池（可動用資產）0% 成長
退休當年：勞退帳戶價值一次併入投資池
自請領年齡起：勞保年金計入退休後收入
若現金池<0：賣出投資池補足（退休後靠此提領）
```
**「用實際報酬計算成長」（一鍵）：** 既有部位以各資產預期報酬市值加權、定期投入以投資配置預期報酬。

### 3.8 退休 / FIRE
```
退休目標（4% 法則） = 今日年支出 × (1+通膨)^(退休−現在) × 25
```

### 3.9 情境模擬
| 情境 | 報酬率 | 通膨 |
|---|---|---|
| 保守 | −2% | +0.5% |
| 中性 | 設定值 | 設定值 |
| 樂觀 | +2% | — |

### 3.10 負債攤還
逐月本息計算，還清即停止。

### 3.11 分期採購
頭期款一次流出；月付金（本利攤還）折入時間軸。

### 3.12 子女養育成本模型
依年齡累計費用，支出與補助分區：一次性生育補助、按月育兒津貼（含年齡上限）、教育補助。

### 3.13 投資配置（5 類資產）
```
假設報酬：股7% / 債3.5% / REITs6% / 黃金3% / 現金1%（可調）
組合預期報酬 = Σ(類別權重 × 類別報酬)
四階段：成長資產(股+REITs) <30%保守 / 30–55%穩健 / 55–78%成長 / ≥78%積極
```

### 3.14–3.15 資金流量
- **本月典型月**：每日滾動餘額、最低餘額日
- **全年各月**：12 個月長條圖、最緊月份警示

### 3.16 郵局薪級模型（114/1/1 待遇表）
```
月薪 = 職階薪額(薪級) + 職位加給(職責層次)
碰頂後每年 +1 個月；逢每5年年資年 +2 個月
年收入加總（現在至退休）與平均年薪顯示於面板底部
```
**預設值（v3.4 更新）：** 專業職(二) / 薪級55 / 職層3 / 115年入局。

### 3.17 台灣在地化說明
勞保年金、勞退新制已實作（簡化估算）；稅務未自動計算；蒙地卡羅已實作。

### 3.18 勞保老年年金
```
月年金 = max( 平均月投保薪資 × 年資 × 1.55% ,
             平均月投保薪資 × 年資 × 0.775% + 3,000 )
```

### 3.19 勞退個人專戶
```
每月提繳 = 月提繳工資 × (雇主6% + 自提0–6%)（上限150,000）
退休帳戶價值 = 現有餘額×(1+r)^年數 + 提繳金額之年金終值
```

### 3.20 保險防護（壽險缺口）
```
保障缺口 = (家庭生活費+負債+子女大學金+喪葬) − (總資產+既有壽險保額)
```

### 3.21 蒙地卡羅退休模擬
```
800 次模擬，當年報酬 = baseRet + σ × randn()
σ = Σ(配置權重 × 波動率)：股18% / 債7% / REITs15% / 黃金16% / 現金1%
退休成功機率 = 活到預期壽命時資產>0 的比例
```

---

## 4. 資料模型（實際 Supabase 結構）

全部啟用 **Row Level Security（RLS）**，政策為 `auth.uid() = user_id`。

| 資料表 | 主要欄位 |
|---|---|
| `fp_settings` | user_id、ages、rates、monthly_invest、child_costs(jsonb)、postal(jsonb)、ar_*/pf_*（5類報酬與配置）、use_real_return、li_*/lp_*/ins_* |
| `fp_incomes` | name、amount、frequency、growth_rate、pay_day、pay_month、sort_order |
| `fp_expenses` | name、category、amount、frequency、is_fixed、pay_day、pay_month、sort_order |
| `fp_assets` | name、type、value、expected_return、is_liquid、exclude_projection、sort_order |
| `fp_liabilities` | name、type、balance、interest_rate、monthly_payment、sort_order |
| `fp_goals` | name、target_amount、target_years、priority、start_amount、return_rate、cycle_years、sort_order |
| `fp_children` | name、current_age、各階段費用欄位、sort_order |
| `fp_purchases` | name、price、down_payment_pct、loan_years、interest_rate、purchase_in_years、sort_order |

### 4.1 商業化資料表

| 資料表 | 說明 |
|---|---|
| `fp_plans` | 方案定義（free/full/subscription）；features jsonb 為允許使用的 tab key 陣列 |
| `fp_subscriptions` | 每用戶一筆；status: active/trial/expired/cancelled；前端唯讀 |
| `fp_user_overrides` | 管理員對特定用戶臨時開關功能 |
| `fp_snapshots` | 完整 state 快照（歷史規劃紀錄） |
| `fp_addons` | 加購模組目錄：postal、startup、realestate（開發中）、itax（開發中）|
| `fp_user_addons` | 用戶已購模組；null expires_at = 買斷；**訂閱到期後仍保留** |
| `fp_site_config` | 站點設定：收款資訊、定價、聯絡方式、追蹤通知連結（含 label/note） |

### 4.2 fp_site_config 說明（可透過 admin.html 調整）

| key | 說明 | 目前值 |
|---|---|---|
| `payment.bank_name` / `bank_code` | 銀行名稱/代碼 | 永豐銀行 807 |
| `payment.account_no` / `account_name` | 帳號 / 戶名 | 19901800073505 / 鄭家弦 |
| `payment.price_full` | 完整版買斷價（NT$） | 1,980 |
| `payment.price_sub` | 訂閱制月費（NT$） | 99 |
| `contact.email` | 聯絡信箱 | jcn.mlm@gmail.com |
| `contact.response_time` | 開通回應時間 | 1-2 個工作天 |
| `site.newsletter_url` | 開發中模組追蹤更新連結 | （待填入）|

> **定價以 Supabase `fp_site_config` 為準**，勿在程式碼中硬寫金額。

### 4.3 Feature Flags 運作
登入後呼叫 Edge Function `get-features`，回傳 `{ features, plan, addons }`。`canAccess(tab)` / `hasAddon(key)` 控制存取；**addon-store tab 永遠對所有用戶開放**（繞過 canAccess）。

---

## 5. 系統架構（實際採用）

```
[ 公開頁面 ]
  landing.html  → 行銷頁、定價、FAQ、所有 CTA 導向 index.html#login
  terms.html    → 使用者條款（#terms）＋隱私政策（#privacy）
  index.html    → 主應用程式（15 個功能分頁）

[ 本機工具 ]
  admin.html    → 後台管理（不部署，含 service_role 功能）

[ 資料層 ]
  本地模式：localStorage（LS_KEY = 'shizhi_finance_v1'）
  雲端模式：Supabase（PostgreSQL + Auth + RLS）
    ├ anon key（前端安全可公開）
    ├ Edge Function：get-features
    └ service_role（僅 admin.html 本機使用）

[ 部署 ]
  GitHub Pages（index.html、landing.html、terms.html）
  admin.html 列入 .gitignore，不部署
```

---

## 6. 使用者流程與介面

### 6.1 訪客流程
```
landing.html → 了解產品 / 定價
  → 任一 CTA「免費開始使用」/ 「最划算方案」/ 「立即訂閱」
  → index.html#login → 設定頁自動捲至雲端同步區（#cloud-area）
  → 註冊帳號（勾選使用者條款必填、行銷通知選填）
  → 登入後 fetchFeatures() 取得方案權限 → rerenderActiveTab() 更新畫面
```

### 6.2 升級流程
1. 任一付費牆觸發（報告鎖定、PDF 按鈕、側邊欄 Widget、模組商店）
2. `showPaymentUpgrade(triggerFeature)` 開啟升級 Modal（標題依觸發點動態切換）
3. Modal 顯示完整版與訂閱制兩個方案卡（NT$1,980 買斷 / NT$99/月起）
4. 顯示銀行匯款資訊（從 Supabase 即時讀取）
5. 填寫匯款末五碼→送出申請→管理員在 admin.html 手動開通
6. 用戶重新登入或點「重新整理」→ `fetchFeatures()` 取得新方案

### 6.3 模組加購流程
（需先有完整版或訂閱制）
1. 模組商店→選擇模組→「加購此模組」
2. 填寫申請表（匯款末五碼）→送出
3. 管理員在 admin.html 授予 `fp_user_addons` 記錄
4. 加購模組**永久有效**，訂閱到期後仍可使用

### 6.4 主要 UI 設計原則
- 設計風格：溫暖紙感（`--bg: #F6F2E9`）× 沉穩翠綠（`--primary: #1E6B50`）
- 表格即時編輯，自動重算
- 所有假設透明可調
- 全站置入免責聲明；條款連結出現在升級 Modal、設定頁免責聲明、Landing Page 頁尾

---

## 7. 建議報告與建議引擎

### 7.1 報告結構（四段，可列印/存 PDF）
1. 財務現況體檢（**免費可看**）
2. 退休準備度（三情境 vs 4% 目標）🔒 完整版
3. 目標可行性與資金分配（瀑布式）🔒 完整版
4. 行動建議（規則式引擎）🔒 完整版

### 7.2 建議引擎規則
入不敷出、緊急預備金不足、儲蓄率 <20%、高利率（>6%）負債、未設定定期定額、退休準備缺口、目標所需高於可投入。

### 7.3 付費牆設計
- **純前端 CSS 漸層遮罩**：第二節起模糊並疊加升級 CTA
- 選擇此方案的原因：報告內容來自用戶自身資料，繞過只能看到自己的數字，無資料洩漏風險；付費核心已轉移至雲端同步
- PDF 匯出另外計入 `report_export` feature flag

---

## 8. 商業化策略（v3.4 更新）

> 以下為產品/營運建議，非法律意見。**收費前務必諮詢台灣金融法規律師與會計師/記帳士。**

### 8.1 方案層級

| 方案 | 功能 | 定價 |
|---|---|---|
| 免費 | 6 個基礎功能 + 模組商店瀏覽 | NT$0 |
| 完整版（買斷） | 所有主功能 + PDF 匯出 + 進階模組 | NT$1,980（早鳥優惠，未來可能調漲） |
| 訂閱制（月費） | 完整版 + 歷史快照 | NT$99/月起（3個月9折/6個月85折/12個月8折） |
| 加購模組 | 需先有完整版或訂閱制 | 各模組獨立定價（如郵局薪資試算 NT$499） |

> 「買斷」為**功能使用授權**，非購買原始碼或著作權；功能持續更新，早買早鎖定優惠價。
> 定價以 Supabase `fp_site_config` 為準，勿硬寫在程式碼或文件中。

### 8.2 付款流程（目前：人工確認）
```
用戶填申請表（匯款末五碼）→ 管理員在 admin.html 確認 → 手動開通方案
```
未來：串接 ECPay / Stripe + webhook 自動寫入訂閱狀態。

### 8.3 轉換 UX
- **側邊欄 Widget**（永久可見）：所有頁面都看得到升級入口
- **自然節點升級提示**：報告鎖定、PDF 按鈕、進階模組頁
- **Landing Page CTA**：`landing.html` 所有按鈕導向 `index.html#login`
- **模組商店**：所有用戶可瀏覽，看到功能後再引導升級

### 8.4 行銷同意機制
- 註冊時選填「同意接收產品更新與優惠通知」
- 同意與否存入 Supabase user metadata（`marketing_consent: true/false`）
- 行銷信只能寄給 `marketing_consent=true` 的用戶（台灣個資法）
- 財務數字（收入/資產/負債/目標金額）= 敏感個資，絕不用於行銷分析

### 8.5 追蹤更新通知
開發中模組（房地產、綜所稅等）頁面的「追蹤更新通知」按鈕，連結由 `fp_site_config.site.newsletter_url` 控制，可在 admin.html → 📡 模組更新通知 設定（留空則按鈕不顯示）。

### 8.6 法規定位
- 安全策略：賣「工具/便利」（SaaS 功能），不賣「投資建議」
- 建議報告維持「資產類別層級、不指名標的」
- 服務條款明確排除「個人化投資建議」，標示為教育性試算
- 收費前取得法律意見（尤其投資配置 / 建議報告頁）

### 8.7 收費前檢核清單
- [ ] 律師確認金融法規定位
- [ ] 會計 / 電子發票 / 營業登記安排
- [ ] 金流串接（ECPay/Stripe）+ webhook 自動寫入訂閱狀態
- [ ] 付費報告移至 Edge Function（伺服器端產生）
- [ ] 行銷文案不得保證報酬

---

## 9. 法規遵循與風險

> 一般性整理，非法律意見。

1. **核心紅線**：收費 + 推介**特定有價證券** → 可能構成投顧業務，須金管會核准。
2. **投資配置頁**：資產類別層級的教育性參考，不推介個股。
3. **個資法（PDPA）**：財務數字為敏感個資；需符合告知同意、最小化、加密；已實作「同意機制」與「財務數字不用於行銷」原則。
4. **使用者條款**：`terms.html` 已含服務範圍、禁止行為、反盜版、賠償責任限制、管轄法院（臺灣高雄地方法院）。
5. **隱私政策**：不硬性承諾不使用第三方分析工具（若未來引入 GA 等，將告知並取得同意）。

---

## 10. 開發路線圖（v3.4 更新）

| 階段 | 目標 | 狀態 |
|---|---|---|
| Phase 0：計算邏輯驗證 | 以測試案例驗證公式 | ✅ 完成 |
| Phase 1：MVP | 收支、淨值、目標、退休、儀表板 | ✅ 完成 |
| Phase 2：進階分析 | 多目標瀑布、三情境、PDF、投資配置、子女、分期、流量 | ✅ 完成 |
| Phase 2.5：精準與在地化 | 郵局薪資、勞退勞保、保險防護、全年流量、蒙地卡羅 | ✅ 完成 |
| Phase 2.6：商業化基礎 | 方案/訂閱/加購六張表、Edge Function、feature flags、後台管理工具 | ✅ 完成 |
| **Phase 2.7：前台商業化（v3.4）** | Landing Page、使用者條款、建議報告付費牆、升級 UX、方案 Widget、模組商店全開放、行銷同意機制 | ✅ 完成 |
| Phase 3：精算強化 | 稅務（綜所稅）、XIRR、勞保進階 | ⬜ 未做 |
| **Phase 4：商業化完整** | 金流串接（ECPay/Stripe）+ webhook 自動訂閱、付費報告移至 Edge Function、電子發票 | 🚧 架構已建，金流未串 |
| **Phase 5：對外上線** | 法律意見/合規、會計/登記、隱私權/條款完善、行銷獲客 | ⬜ 未做 |

---

## 11. 已知限制與尚未實作

- 稅務（綜所稅）自動計算（目前需手動以支出近似）。
- XIRR 不規則現金流計算。
- 勞保/勞退為簡化估算（未計提前/延後請領增減給、月退未計年金利率）。
- 蒙地卡羅波動率為加權平均，未計資產間相關係數。
- 保險防護為簡化壽險需求估算，不含失能/醫療/長照精算。
- 郵局模型不自動模擬升遷與考績差異；薪級表依 114/1/1，調整需更新。
- **商業化**：方案/加購架構已建；**金流串接、webhook 自動開通尚未完成**，目前人工確認；付費報告仍在前端產生。
- 第三方分析工具（GA/FB Pixel）尚未安裝；若未來安裝，需更新隱私政策並取得用戶同意。
- 追蹤更新通知連結（`site.newsletter_url`）尚未填入，需在 admin.html 設定。

---

## 12. 風險與成功關鍵

**主要風險**：法規定位（對外收費時最高優先）、計算正確性、資安與個資、付費牆可被前端繞過（需伺服器端驗權）、人工付款流程無法擴展（需串接金流）。

**成功關鍵**：計算正確且可驗證；假設全程透明；合規定位清楚；轉換 UX 自然（Widget 永久可見、自然節點提示）；付費價值有感（雲端同步、完整報告、加購模組）。

---

## 附錄 A：計算公式速查表
```
年化金額          = 金額 × {每月12, 每季4, 每年1}
平均每月結餘      = 收入 − 支出 − 負債還款
儲蓄率            = (年收入 − 年支出 − 年還款) / 年收入
淨值              = 總資產 − 總負債
緊急預備金(月)    = 可動用資產 / 每月支出
終值              = PV × (1+r)^n
達標每月投入      = 剩餘目標 × rm / ((1+rm)^n − 1)
退休目標(4%法則)  = 今日年支出 × (1+通膨)^(退休−現在) × 25
分期月付          = 貸款額 × (r/12) / (1 − (1+r/12)^(−月數))
郵局年薪          = (職階薪額+職位加給) × (12 + 獎金月 + 碰頂月)
勞保月年金        = max(薪資×年資×1.55%, 薪資×年資×0.775%+3000)
勞退帳戶          = 餘額×(1+r)^年數 + 提繳金額年金終值
壽險缺口          = (年支出×年數+負債+子女大學金+喪葬) − (資產+既有保額)
組合預期報酬      = Σ(類別權重 × 類別報酬)
蒙地卡羅當年報酬  = baseRet + σ × randn()，σ = Σ(配置權重×波動率)
退休成功機率      = 活到預期壽命時資產>0 的次數 / 800
```

## 附錄 B：資料表速查
**主要財務表：** `fp_settings`、`fp_incomes`、`fp_expenses`、`fp_assets`、`fp_liabilities`、`fp_goals`、`fp_children`、`fp_purchases`

**商業化表：** `fp_plans`、`fp_subscriptions`、`fp_user_overrides`、`fp_snapshots`、`fp_addons`、`fp_user_addons`、`fp_site_config`

## 附錄 C：部署說明

| 檔案 | 部署位置 | 說明 |
|---|---|---|
| `index.html` | GitHub Pages 根目錄 | 主應用程式 |
| `landing.html` | GitHub Pages 根目錄 | 行銷 Landing Page |
| `terms.html` | GitHub Pages 根目錄 | 使用者條款與隱私政策 |
| `admin.html` | 本機（.gitignore 排除）| 後台管理，含 service_role 功能 |

**URL：** `https://jcnmlm-ui.github.io/shizhi/`

---

*免責聲明：本文件為產品規劃與實作說明，其中財務計算為通用方法、法規與商業化說明為一般性整理，均不構成投資建議或法律意見。產品所有未來金額皆為依使用者輸入之假設所做的估算，並非保證。重大財務決策與任何對外營運前，請諮詢合格之財務、稅務與法律專業人士。*
